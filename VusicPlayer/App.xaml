<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="VusicPlayer.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VusicPlayer"
    RequestedTheme="Dark">

    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            <!--<AcrylicBrush x:Key="GlassButtonBackground"
      Opacity="0.4"
        TintColor="Transparent"
        TintOpacity="0"
        TintLuminosityOpacity="0.2"
        FallbackColor="Transparent" />
            <Style x:Key="GlassButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource GlassButtonBackground}"/>
                <Setter Property="BorderBrush" Value="White"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="12,8"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <ContentPresenter x:Name="ContentPresenter"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    Content="{TemplateBinding Content}"
                    CornerRadius="{TemplateBinding CornerRadius}"
                    Padding="{TemplateBinding Padding}"
                    HorizontalContentAlignment="Center"
                    VerticalContentAlignment="Center">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="ContentPresenter.Background" Value="{ThemeResource SystemAccentColorLight2}"/>
                                                <Setter Target="ContentPresenter.Opacity" Value="0.8"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="ContentPresenter.Opacity" Value="0.5"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                            </ContentPresenter>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>-->
            <local:BoolToOpacityConverter x:Key="BoolToOpacityConverter"
                                       />
            <local:BoolToTextConverter x:Key="FavToolTipConverter" 
                               TrueText="Remove from Favourites" 
                               FalseText="Add to Favourites"/>
            <local:BoolToTextConverter x:Key="FavGlyph" 
                      TrueText="&#xEB52;" 
                      FalseText="&#xEB51;"/>
            <local:BoolToBrushConverter x:Key="FavColor" 
                      TrueBrush="Red" 
                      FalseBrush="White"/>
        </ResourceDictionary>
    </Application.Resources>

</Application>
