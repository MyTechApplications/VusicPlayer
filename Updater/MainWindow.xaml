<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Updater.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Updater"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Updater">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="20" Orientation="Vertical">
                <ProgressRing IsActive="True" Width="60" Height="60" />
                <TextBlock Text="Applying Updates..." Style="{StaticResource TitleTextBlockStyle}" />
                <TextBlock Text="Please wait while we get Vusic Player ready." Foreground="Gray" />
                <ProgressBar x:Name="UpdateProgressBar"
             Minimum="0"
             Maximum="100"
             Height="6"
             Width="300"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
